# docs — {{name}}

Minimal Node service. Config, typed client, DB/storage, and observability wiring arrive with the
Phase-5 TypeScript SDK (`@mini-cloud/sdk`). See the root `README.md` and `AGENTS.md`.
