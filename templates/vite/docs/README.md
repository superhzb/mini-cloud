# docs — {{name}}

Frontend docs. The typed API client, config, and observability wiring land with the Phase-5
TypeScript SDK (`@mini-cloud/sdk`); until then see the root `README.md` and `AGENTS.md`.
