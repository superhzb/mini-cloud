"""{{name}} — a mini-cloud FastAPI app.

Scaffolded by `mini new`. Uses the mini-cloud SDK for config, database + job queue, object
storage, observability, and inference — no bespoke SQLite/filesystem/inference code. Scores 7/7 on
the scorecard out of the box (`mini score .`).
"""

__version__ = "0.1.0"
