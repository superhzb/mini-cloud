"""mini-cloud scaffolder — `mini new` (Phase 4).

Phase 0 ships this as a skeleton so the shared base tooling config has a real consumer.
The provisioning logic (DB + bucket + brbot-router registration + SDK wiring) lands in Phase 4.
"""

__version__ = "0.0.1"
